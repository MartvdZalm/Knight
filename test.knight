import "program.knight"

fn main(): int
{
    Person person = new Person();
    person.age = 20;
    person.name = "Mart van der Zalm";

    println(person.getName());

    person.print();

    ret 0;
}
